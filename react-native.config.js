module.exports = {
  project: {
    android: {
      // This value is used by the React Native autolinking task when it queries the CLI config.
      // It must match the applicationId defined in android/app/build.gradle
      packageName: 'app.kid_friendly_map_transit_navigator'
    }
  }
};
